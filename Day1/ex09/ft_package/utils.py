def count_in_list(lst: list, item: any) -> int:
    """Count occurences of item in list

    Args:
        lst (list): The list to evaluate
        item (any): Reference item

    Returns:
        int: Number of occurences of item
    """
    return lst.count(item)
